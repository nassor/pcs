[package]
name = "pcs-transformer-parquet"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true
description = "The parquet byte format for PCS: a Transformer reading and writing Parquet with Snappy compression."

[dependencies]
pcs-core        = { path = "../pcs-core", version = "0.1.0" }
pcs-transformer = { path = "../pcs-transformer", version = "0.1.0" }

arrow-array   = { workspace = true }
arrow-schema  = { workspace = true }
arrow-select  = { workspace = true }
bytes         = { workspace = true }
parquet       = { workspace = true }

[dev-dependencies]
tempfile      = { workspace = true }
