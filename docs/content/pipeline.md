+++
title = "Pipeline"
description = "PCS's Pipeline builds a field-level DAG from SystemMeta, topologically sorts it into parallel stages, validates fields against the schema registry, and runs with per-system retry."
template = "pipeline.html"
+++
