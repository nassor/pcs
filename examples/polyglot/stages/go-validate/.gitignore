# Regenerated by `componentize-go bindings` from crates/pcs-guest/wit on every
# build; a committed copy silently desynchronises when pipeline.wit changes.
/wit_exports.go
/pcs_pipeline_types/
/pcs_pipeline_host_io/
/pcs_pipeline_pipeline/

# Copied in by scripts/build-polyglot.sh from examples/polyglot/generated/,
# which the emit command regenerates from the canonical Rust Order definition.
/export_pcs_pipeline_pipeline/schema_gen.go

# Build output.
/validate-go.wasm
